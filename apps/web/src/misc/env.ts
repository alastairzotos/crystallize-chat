
// Obviously replace these with actual environment variables for production
// I don't want to upload a .env to git and don't want whoever's trying this out to make their own
export const env = {
  httpUrl: 'http://localhost:8080',
  wsUrl: 'ws://localhost:8081/chat',
}
